fastapi>=0.110.0
uvicorn[standard]>=0.27.0
pydantic>=2.6.0
pydantic-settings>=2.2.0
python-multipart>=0.0.9
httpx>=0.27.0
chromadb>=0.5.0
rank-bm25>=0.2.2
cryptography>=42.0.0
pypdf>=4.0.0
beautifulsoup4>=4.12.0
lxml>=5.1.0

# V4 — cross-encoder reranker (Stage 2).
# torch is large (~3 GB on first install). On RunPod CUDA wheels are picked
# automatically when a GPU is present.
sentence-transformers>=3.0.0
torch>=2.1.0
